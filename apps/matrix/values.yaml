elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.karlinsec.com"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.karlinsec.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.karlinsec.com
      server_name: karlinsec.com

dendrite:
  dendrite_config:
    global:
      server_name: matrix.karlinsec.com
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.karlinsec.com
    tls:
      - hosts:
          - matrix.karlinsec.com
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
